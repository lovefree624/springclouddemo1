management:
  endpoints:
    web:
      exposure:
        include: bus-refresh
spring:
  application:
    name: eureak-sumer2
  cloud:
    config:
      #分支
      label: master
      #环境
      profile: dev
      #configserver的地址
      uri: http://localhost:6061/
#  config: classpath:logback-boot.xml